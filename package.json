{
  "name": "traxyn",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/api-client",
    "packages/ui"
  ],
  "scripts": {
    "build": "npm run build --workspace @traxyn/api-client && npm run build --workspace @traxyn/web",
    "typecheck": "npm run typecheck --workspace @traxyn/api-client && npm run typecheck --workspace @traxyn/web",
    "dev:web": "npm run dev --workspace @traxyn/web",
    "dev:api": "mvn -f apps/api/pom.xml spring-boot:run",
    "test:api": "mvn -f apps/api/pom.xml test"
  }
}
