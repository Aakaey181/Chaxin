name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  repository-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check foundation files
        run: |
          test -f AGENTS.md
          test -f docs/product/PRD-v0.1.md
          test -f docs/product/backlog.md
          test -f docs/architecture/adr/ADR-001-modular-monolith.md

